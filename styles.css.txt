body {
    font-family: 'Roboto', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #000;
    color: #fff;
    line-height: 1.6;
}

header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
    background-color: rgba(0, 0, 0, 0.9);
    position: fixed;
    width: 100%;
    z-index: 100;
}

.logo h1 {
    font-family: 'Playfair Display', serif;
    font-size: 2em;
    color: #FFD700;
}

nav ul {
    list-style: none;
    display: flex;
    gap: 20px;
}

nav a {
    color: #fff;
    text-decoration: none;
    font-weight: 300;
}

.hero {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(45deg, #000, #333);
    animation: fadeIn 2s ease-in;
}

.hero-content {
    text-align: center;
    max-width: 800px;
}

.hero-image {
    width: 100%;
    max-width: 1200px;
    margin-top: 20px;
    border-radius: 10px;
}

.products, .mega-projects, .about, .contact {
    padding: 100px 20px;
    text-align: center;
    opacity: 0;
    animation: fadeInUp 1s ease-out forwards;
}

.product-grid, .mega-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
    margin-top: 50px;
}

.product-item, .mega-item {
    background-color: rgba(255, 255, 255, 0.1);
    padding: 20px;
    border-radius: 10px;
    transition: transform 0.3s;
}

.product-item:hover, .mega-item:hover {
    transform: scale(1.05);
}

.specs, .specs-art {
    margin-top: 20px;
    display: flex;
    justify-content: space-around;
}

.spec-item {
    text-align: center;
}

.icon {
    font-size: 2em;
}

footer {
    text-align: center;
    padding: 20px;
    background-color: rgba(0, 0, 0, 0.9);
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(50px); }
    to { opacity: 1; transform: translateY(0); }
}

/* Responsive */
@media (max-width: 768px) {
    .product-grid, .mega-grid {
        grid-template-columns: 1fr;
    }
    header {
        flex-direction: column;
    }
}